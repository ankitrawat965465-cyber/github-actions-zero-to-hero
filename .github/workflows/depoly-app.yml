name: Depoly Flask app    

on:
    workflow_dispatch:
  
jobs:
    depoly:
        runs-on: self-hosted

    steps:
        -name: checkoutcode
        uses: action/checkout@v4

        -name:depoly with docker compose
        run: docker compose up -d --build
